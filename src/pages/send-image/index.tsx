import { Layout } from '../../components/Layout';
import { SendImageComponent } from '../../components/SendImage';

export default function SendImage() {
  return (
    <Layout>
      <SendImageComponent />
    </Layout>
  );
}
